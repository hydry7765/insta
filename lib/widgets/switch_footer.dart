import 'package:flutter/material.dart';

Padding SwitchFooter() {
  return Padding(
    padding: EdgeInsets.only(top: 132.0, bottom: 10.0),
    child: Text(
      "Don't have an account?Sign up",
      style: TextStyle(
        color: Colors.white,
        fontSize: 16,
        fontWeight: FontWeight.bold,
      ),
    ),
  );
}
